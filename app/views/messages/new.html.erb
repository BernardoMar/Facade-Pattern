<div class="container">

  <%= form_for(@message) do |f| %>

    <div class="field">
        <div class="create">

          <%= f.label :message %><br>
          <%= f.text_area :content %>

        </div>
      </div>

    <div class="actions">
      <%= f.submit "Create" %>
    </div>
    
  <% end %>
</div>
